[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "yandex-search-cli"
version = "0.2.1"
description = "AI-agent friendly CLI for Yandex Search API — web search, image search, YandexGPT generative search, and Wordstat statistics from the terminal."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [
    { name = "Nolan Vale" },
]
maintainers = [
    { name = "Nolan Vale Tools" },
]
keywords = [
  "yandex",
  "yandex-search",
  "yandex-cloud",
  "yandexgpt",
  "search-api",
  "web-search",
  "russian-search",
  "cli",
  "python-cli",
  "ai-agents",
  "llm-tools",
  "json-output",
  "terminal-tools",
  "automation",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
    "Topic :: Utilities",
]
dependencies = ["requests>=2.28", "defusedxml>=0.7"]

[dependency-groups]
dev = ["ruff>=0.4", "pytest>=8.0"]

[project.urls]
Homepage = "https://github.com/nolan-vale/yandex-search-cli"
Repository = "https://github.com/nolan-vale/yandex-search-cli"
Documentation = "https://github.com/nolan-vale/yandex-search-cli/blob/main/docs/USAGE.md"
Issues = "https://github.com/nolan-vale/yandex-search-cli/issues"
Changelog = "https://github.com/nolan-vale/yandex-search-cli/releases"

[project.scripts]
yandex-search               = "yandex_cli.main:search"
yandex-gen                  = "yandex_cli.main:gen"
yandex-image-search-by-image = "yandex_cli.image_search:search_by_image"
yandex-image-search = "yandex_cli.image_search:search"
yandex-wordstat = "yandex_cli.wordstat:wordstat"

[tool.hatch.build.targets.wheel]
packages = ["src/yandex_cli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
